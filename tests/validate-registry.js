// Stub — real validation added when registry.json is populated (ADR-0002).
// Exits 0 so PR validation passes until then.
console.log("validate-registry: stub — no registry entries to validate yet");
process.exit(0);
